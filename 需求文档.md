# 需求文档（PRD）：弈览 · 云顶之弈攻略 Web 应用

> 版本：v0.3（**纯网页决策版**）
> 状态：形态已定（纯 Web），技术栈方案待确认后执行
> 适用：可直接作为 AI Coding 的输入，用于生成 Next.js 前端 + Serverless 后端
> 方法论来源：知识库《光羽的平行世界》（一人公司篇 / 游戏成功学篇 / 社群篇 / 私域篇）

---

## 1. 项目概述

### 1.1 正式命名
**弈览**（原内部代号，已确认）

### 1.2 一句话定位
**为《云顶之弈》玩家提供"版本同步、即查即用"的阵容 / 装备 / 版本攻略 Web 应用，核心解决攻略永远慢半拍的痛点。**
> 形态决策：**首版为纯网页应用（Web）**，微信小程序暂缓（架构与内容可后续复用，不废弃）。

### 1.3 背景与机会（供需失衡）
- **云顶之弈**每个版本（赛季）羁绊、棋子、海克斯、装备全部重洗，现有攻略更新慢、易过时 → 玩家第一需求是"这版本玩啥、装备怎么合"。
- 竞品（掌上英雄联盟 / 大神 / OP.GG / 多玩 / 捞月狗）普遍重数据、轻"人话点评"，且更新滞后。
- **我们的差异点**：用 AI 在每个 patch 当天自动生成/更新"为什么强、怎么运营、怕什么"的点评，解决时效痛点。

---

## 2. 目标用户

| 维度 | 描述 |
|---|---|
| 核心用户 | 云顶之弈休闲/中度玩家（15–35 岁男性为主），每个版本上线后急需"抄作业" |
| 次要用户（二期） | 英雄联盟玩家，查英雄出装/符文/克制 |
| 痛点场景 | ①版本更新后不知道玩什么阵容 ②装备合成路径记不住 ③想看"人话版"运营思路 |
| 使用频次 | 版本更新周高频（日均多次），平峰期低频查询 |
| 获客渠道 | NGA、百度贴吧、斗鱼/虎牙主播群、B站攻略区、微信游戏圈（链接分享） |

---

## 3. 产品目标与成功指标

### 3.1 北极星指标
**周活跃查询用户数（WAU）+ 阵容收藏率**

### 3.2 关键阈值（生死线，来自一人公司篇的 CAC/LTV 法则）
- 次日留存 ≥ 30%，7 日留存 ≥ 15%（否则产品不成立）
- 付费率 ≥ 10% 才继续投入；< 5% 考虑换方向
- CAC（获客成本）< LTV（每用户价值）

### 3.3 MVP 验证目标
上线 4 周内获取 **1000 个目标用户**，观察点击率 / 留存率 / 卸载率。

---

## 4. MVP 范围

### 4.1 明确包含（首版只做这 4 个，纯云顶之弈 Web）
1. 云顶之弈 **当前版本 T0/T1/T2 阵容速查**
2. **装备合成表**（点装备看合成路径）
3. **版本更新日志**（AI 自动摘要本版本改动）
4. **「我的」**：阵容收藏 + 实战记录/复盘（浏览器本地存储）

### 4.2 明确不包含（二期）
- **英雄联盟板块整体**（英雄库 / 符文 / 出装 / 克制）不进入 MVP，二期再做。
- 微信小程序版本（暂缓，架构可复用）
- 社区 / UGC 阵容共创
- 付费深度内容
- AI 私域客服

---

## 5. 核心功能详述

### 5.1 云顶之弈阵容速查
- 列表页按 **版本 + 强度档位（T0/T1/T2）** 排序。
- 每个阵容卡包含：核心棋子、羁绊组合、核心装备、运营节奏（几人口 D 牌）、克制与被克。
- 点击进入阵容详情页，展示：站位图（占位图，先用静态示意图）、装备分配、过渡思路、怕什么阵容。
- 数据来源优先级：**OP.GG 抓取 > 掌盟抓取 > 社区投稿**；Riot 官方 TFT API 作为二期备选（需申请 key）。

### 5.2 装备合成表
- 全装备网格，点击任一装备 → 弹出**合成路径树**（小件 → 大件）。
- 反向查询：选两个小件 → 显示能合成的大件。
- 数据：TFT 装备固定，可静态配置（无需实时 API）。

### 5.3 版本更新日志
- 每个 patch 上线后，自动抓取官方改动 → LLM 生成"人话摘要"（哪些阵容被削/增强、新装备）→ 人审后发布。
- 列表按版本号倒序，置顶当前版本。

### 5.4 「我的」：收藏 + 实战复盘
- **收藏**：点击收藏阵容、装备，存浏览器 localStorage（MVP 不跨设备）。
- **实战复盘**（呼应游戏成功学篇"看攻略→实践→复盘"闭环）：
  - 用户记录一局：选用阵容、最终排名（1–8）、关键节点备注。
  - 按阵容聚合本机历史排名分布，给出"你用这套阵容平均第几名"。
  - 差异化留存功能，让攻略从"看"变成"练+复盘"。

---

## 6. 信息架构 / 页面结构

```
/                        首页：阵容速查列表（按版本/T档筛选）+ 搜索 + 版本切换
/comp/[id]               阵容详情：站位图 + 羁绊/装备/运营/克制 + [收藏][记一局]
/items                   装备库网格
/item/[id]               装备详情：合成树 + 反向查询
/versions                版本列表
/version/[no]            版本改动详情
/mine                    我的：收藏 + 实战复盘记录 + 设置
```

页面元素规格（供前端）：
- 列表卡：标题（阵容名）+ 强度标签（T0 红/T1 橙/T2 灰）+ 3 个核心棋子缩略 + 收藏图标。
- 详情页：顶部站位图（占位 750×750 或响应式），下方分段卡片（羁绊/装备/运营/克制）。

---

## 7. 数据模型

### 7.1 静态种子数据（MVP，JSON 文件）
```
versions.json   [{ versionId, name, patchNo, releaseDate, summary, isCurrent }]
comps.json      [{ compId, versionId, name, tier, coreChampions[], traits[],
                   coreItems[], earlyGame, midGame, lateGame, counters[], counteredBy[],
                   站位图url, aiComment, source, updatedAt }]
items.json      [{ itemId, name, tier, recipe[]{itemId}, buildFrom[]{itemId} }]
```

### 7.2 用户数据（MVP，浏览器 localStorage）
```
yilan:fav        { favComps[], favItems[] }
yilan:records    [{ compId, rank(1-8), note, createdAt }]
```
> 二期如需跨设备同步，引入 Supabase / 云数据库，模型同 §7.2。

---

## 8. 内容生产流程（AI 自动 + 人审）

1. **抓取**：版本发布后，Serverless 定时任务**抓取 OP.GG** 阵容/胜率数据（Riot 官方 API 二期备选）。
2. **生成**：调用**腾讯混元**（服务端）根据数据产出"强度评级 + 运营点评 + 克制关系 + 人话摘要"。
3. **人审**：管理员后台审核后写入 `comps` / `versions`（首版可先半自动，人工确认）。
4. **发布**：前端读取 `isCurrent` 版本数据自动展示。
5. **UGC（二期）**：玩家投稿阵容 → 社区投票 → 采纳进榜。

---

## 9. 技术栈与架构（纯网页方案）

| 层 | 选型 | 理由 |
|---|---|---|
| 前端框架 | **Next.js（React，App Router）** | 你熟 React 心智；前端+后端同仓，部署简单 |
| 后端 | **Next.js API Routes / Serverless** | 处理 OP.GG 抓取 + **混元代理**（key 仅服务端，禁止进前端） |
| 数据源 | **OP.GG 抓取**（首版）→ Riot API（二期） | 阵容与版本数据 |
| AI 生成 | **腾讯混元**（服务端调用） | 生成阵容点评与版本摘要，与生态契合 |
| 数据存储 | MVP：**静态 JSON + localStorage**；二期：Supabase / 云数据库 | MVP 零数据库成本，快速验证 |
| 托管 | **Vercel / Netlify / CloudStudio**（静态+Serverless，免费或极便宜） | 免运维；详见 §10 备案说明 |

### 9.1 目录约定（Next.js）
```
app/
  page.tsx                首页（阵容速查）
  comp/[id]/page.tsx      阵容详情
  items/page.tsx          装备库
  item/[id]/page.tsx      装备详情
  versions/page.tsx       版本列表
  version/[no]/page.tsx   版本详情
  mine/page.tsx           我的
components/               CompCard, ItemGrid, TierTag, StationMap
lib/                      opgg.ts(抓取), hunyuan.ts(代理), storage.ts(localStorage)
data/                     versions.json, comps.json, items.json（种子）
app/api/
  fetch-opgg/route.ts     抓取 OP.GG
  ai-comment/route.ts     混元代理（key 服务端）
```

---

## 10. 非功能需求与合规

- **性能**：首页列表首屏 < 1s（静态数据 + CDN）；阵容详情 < 1.5s。
- **备案 / 托管**：纯网页**无需微信类目**。若用 mainland 服务器 + 自定义域名需 **ICP 备案**（需企业/个体户主体，约 1–2 周）；海外托管（Vercel/Netlify）免备案但国内访问偏慢。MVP 可用平台免费子域先验证。
- **安全**：混元 API key 仅存服务端环境变量，前端绝不暴露；OP.GG 抓取遵守其 ToS，控制频率避免封禁。
- **合规**：含 UGC 时需内容审核（二期）；数据来源注明出处。
- **更新**：每个 patch 24h 内完成阵容/日志更新。

---

## 11. 里程碑与验收

| 周 | 任务 | 验收标准 |
|---|---|---|
| W1 | 需求文档(v0.3) + 纸面原型 | 本文档评审通过 |
| W2 | Next.js 骨架 + 种子数据 + OP.GG 抓取脚本 + 混元代理 | 本地 `npm run dev` 可跑通 |
| W3 | MVP（阵容速查+装备表+日志+收藏/复盘）部署 Vercel 预览 | 4 个 MVP 功能可用 |
| W4 | 获 1000 目标用户，看留存埋点 | 次日留存≥30% |
| W5+ | 迭代 + 二期（英雄联盟库/社区/UGC/付费/小程序） | 付费率≥10% 再投入 |

---

## 12. 风险与备选

- **数据源封锁**：OP.GG 反爬/限流 → 备选：掌盟抓取 + 人工录入 + 社区 UGC 兜底；二期接 Riot 官方 API。
- **国内访问慢**：Vercel 在大陆偶有波动 → 备选部署 CloudStudio / 腾讯云静态（需备案）。
- **攻略同质化**：强化"实战复盘"留存功能作为壁垒。
- **算账不过**：付费率<10% 则按一人公司篇"大胆放弃换方向"。

---

## 13. 决策记录

### 13.1 命名
✅ **弈览**（6 选 1 已定）。其余 5 个（云顶抄作业/一图流/上分弈站/阵容日历/版本弈闻）保留作备选 / 矩阵名。

### 13.2 LLM
✅ **腾讯混元**（与生态契合，MVP 先用它跑通）。备选：DeepSeek（更便宜）、通义/文心/OpenAI。

### 13.3 微信类目（仅未来做小程序时才需考虑）
若后续做小程序：注册需声明类目（「工具」或「资讯」），且「资讯」需企业/个体户主体；首版纯网页不受此限。

### 13.4 已确认决策
- ✅ **数据源**：先用 OP.GG 抓取（Riot 官方 API 二期备选）。
- ✅ **MVP 范围**：英雄联盟板块整体不进 MVP，首版纯云顶之弈。
- ✅ **形态**：**纯网页应用（Web）**，微信小程序暂缓（不废弃，架构可复用）。

### 13.5 待确认（执行前最后一步）
- ⏳ 技术栈方案（§9）确认后，开始搭 Next.js 骨架。
- ⏳ 托管平台选择：Vercel（快、海外）/ CloudStudio（大陆优、需备案）— MVP 先用 Vercel 免费层验证。
